import * as tslib_1 from "tslib";
import "../../CommonImports";
import "../../Core/core.css";
import "./FabricIcons.css";
import * as React from "react";
import { Tooltip } from '../../TooltipEx';
import { css, getSafeId } from '../../Util';
export function Icon(props) {
    if (props.render) {
        return props.render(props.className);
    }
    var iconWrapperProps = {
        id: getSafeId(props.id),
        key: props.key,
        onClick: props.onClick,
        onMouseDown: props.onMouseDown,
        role: props.role ? props.role : props.ariaLabel || props.ariaLabelledBy ? "img" : undefined,
        style: props.style,
        tabIndex: props.tabIndex,
        title: props.title
    };
    var iconClassName = css(props.className, "flex-noshrink", props.iconName && "fabric-icon", props.iconName && "ms-Icon--" + props.iconName, props.size);
    var icon = props.ariaLabel || props.ariaLabelledBy ? (React.createElement("span", tslib_1.__assign({ "aria-label": props.ariaLabel, "aria-labelledby": getSafeId(props.ariaLabelledBy) }, iconWrapperProps),
        React.createElement("span", { "aria-hidden": "true", className: iconClassName }))) : (React.createElement("span", tslib_1.__assign({ "aria-hidden": "true", className: iconClassName }, iconWrapperProps)));
    if (props.tooltipProps) {
        icon = React.createElement(Tooltip, tslib_1.__assign({}, props.tooltipProps), icon);
    }
    return icon;
}
